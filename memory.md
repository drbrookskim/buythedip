# Buy The Dip (눌림목 매매) 전략 & 시스템 개발 메모리

## 📌 작업 일시
- **최종 업데이트**: 2026-08-17 (월요일)

---

## 🚀 프로젝트 개요
주식 눌림목(Buy the Dip) 매수 및 다각적 매도 전략(익절, 손절, 트레일링 스탑, 타임컷) 연구, 백테스팅, 리스크 관리 및 실제 주가 데이터 연동 시뮬레이션 웹 대시보드 구축.

---

## 📝 2026-08-15 주요 작업 내역

### 1. 초기 투자금 천 단위 쉼표(`,`) 실시간 포맷터 적용
- `10000000` -> `10,000,000` 등 입력 및 수정 시 실시간으로 천 단위 구분 쉼표(`,`) 자동 포맷팅.
- 단위 배지(`원`) 및 `tabular-nums` 가독성 타이포그래피 적용.
- 백테스트 엔진에서 쉼표가 포함된 문자열을 숫자로 안전하게 파싱(`replace(/,/g, '')`) 처리.

### 2. 빠른 전략 추천 각 항목 상세 설명 및 인터랙션 강화
- **4가지 프리셋 카드화**:
  - `기본 균형형` (표준 추천): 익절 6% / 손절 3% + 7일 타임컷 & 3% 트레일링 스탑 병행 표준 전략.
  - `트레일링 추종형` (수익 극대화): 목표가 12% & 고점 대비 -2.5% 트레일링 청산으로 대세 상승 추세 추종.
  - `단기 타임컷형` (단기 회전): 과매도(RSI 40) 빠른 4% 반등 노림 & 4일 미상승 시 즉시 청산.
  - `고손익비 스윙` (손익비 3.2:1): 120일선 위 급락 조정 시 진입하여 익절 8% / 손절 2.5% 고손익비 추구.
- **실시간 상세 안내 배너 (`preset-detail-box`)**: 프리셋 선택 시 해당 전략의 핵심 매수 조건 및 청산 로직에 대한 상세 가이드를 직관적으로 안내.

### 3. 매도 타점 시각화 오류 해결 및 체결가 로직 고도화
- **원인 분석**:
  - 차트 범례 상 🔵 익절(TP), 🔴 손절(SL), 🟠 트레일링(TS), 🟣 타임컷(TC)으로 구분되어 있었으나, 기존 `app.js`에서 모든 청산 포인트를 단일 `sellMarkers` 데이터셋(🔴 빨간색 원 `#e11d48`)으로 일괄 렌더링하고 있었음.
  - 이로 인해 매수가보다 높은 위치에서 발생한 익절(TP), 트레일링 청산(TS), 타임컷(TC)까지 전부 **빨간색(손절)** 원으로 표시되어 사용자가 "매수가 위에서 손절이 나간 것"으로 오인하는 문제가 발생함.
- **개선 및 정상화**:
  - **마커 데이터셋 5종 완전 분리**:
    - 🟢 `매수 (Dip Buy)`: `#059669` (삼각형)
    - 🔵 `익절 (Take Profit)`: `#0284c7` (원)
    - 🔴 `손절 (Stop Loss)`: `#e11d48` (원)
    - 🟠 `트레일링 (Trailing Stop)`: `#d97706` (원)
    - 🟣 `타임컷 (Time Cut)`: `#7c3aed` (원)
  - **체결가(Execution Price) 정밀화**: 갭상승/갭하락 시 시가(`bar.open`) 체결, 장중 도달 시 지정가/트레일링 가격(`tpPrice`, `slPrice`, `tsPrice`) 정확 매핑.
  - **툴팁 인포메이션 고도화**: 마우스 호버 시 매수 진입가 및 청산 사유별 체결 가격을 한눈에 식별 가능하도록 포맷 개선.

### 4. 핵심 매도 전략 개념 가이드 (트레일링 스탑 & 타임컷)
- **트레일링 스탑 (Trailing Stop, 추적 청산)**:
  - 주가 상승 시 매도 기준선도 따라 상향되며, 진입 후 기록한 최고점 대비 N%(예: 3%) 하락 시 자동 청산.
  - 대세 상승장의 폭등 시세를 끝까지 추종(Let profits run)하면서 고점 꺾임 시 이익을 보존.
- **타임컷 (Time Cut, 시간 기반 청산)**:
  - 매수 후 N일(예: 7영업일) 동안 목표 익절가에 도달하지 못하고 지지부진하게 횡보할 경우 시장가로 전량 매도.
  - 자금이 묶이지 않도록 기회비용을 절감하고, 장기 횡보 후 급락하는 리스크를 사전 차단.

### 5. Yahoo Finance 미완성 일봉 NaN으로 인한 JSON 파싱 에러 해결
- **원인 분석**:
  - 장 마감 직후 또는 휴일/장전 시간대에 Yahoo Finance API(`yfinance`)가 당일 일봉 행의 OHLCV 값을 `NaN`으로 반환함.
  - Python `json.dumps()`는 기본적으로 `NaN` 값을 표준 JSON 규격(RFC 8259)에 위배되는 `"open": NaN` 형태의 토큰으로 직렬화하여 브라우저 `response.json()`에서 `SyntaxError: Unexpected token 'N'` 파싱 에러가 발생함.
- **해결 조치**:
  - **백엔드 (`server.py`)**:
    - `df.dropna(subset=['Open', 'High', 'Low', 'Close'])` 및 `math.isnan()`, `math.isinf()` 검증으로 미완성/NaN 일봉 데이터 원천 제거.
    - `json.dumps(..., allow_nan=False)` 엄격 모드 적용으로 비표준 JSON 출력 원천 차단.
    - 종목 파라미터로 `삼성전자 (005930)` 같은 문자열이 직접 전달되어도 6자리 코드(`005930`)를 정규식으로 자동 추출하도록 심볼 해석 강화.
  - **프론트엔드 (`app.js`)**:
    - 수신 데이터 필터링 시 `!isNaN(item.close) && item.close > 0` 방어 로직 추가.
    - 검색창 직접 입력 시 자동 정규식 심볼 추출 적용.

### 6. 타점 마커 디자인 개편 (Dot & Triangle 정밀 시각화)
- **적용 마커 매핑**:
  - 🔴 **매수 (Buy)**: `red dot` (채워진 빨간 원, `#e11d48`)
  - 🔵 **익절/매도 (TP)**: `blue dot` (채워진 파란 원, `#0284c7`)
  - ⭕ **손절 (SL)**: `empty red dot` (빨간 테두리 빈 원, 흰색 내부 + 빨간 보더)
  - 🟢 **트레일링 (TS)**: `green triangle` (초록색 상향 삼각형 `▲`, `#059669`)
  - 🔵 **타임컷 (TC)**: `empty blue dot` (파란 테두리 빈 원, 흰색 내부 + 파란 보더)
- **적용 범위**:
  - **차트 좌측 상단 범례 (`.chart-title-area`, `.chart-legend-custom`)**: 차트 좌측 상단 타이틀 옆에 직관적으로 배치하고, 10px 크기의 선명한 도트 및 트라이앵글 벡터 뱃지로 대폭 확대하여 각 타점을 한눈에 즉시 식별 가능하도록 개선.
  - **Chart.js 캔버스 포인트 마커 (`pointStyle`)**: 캔들/라인을 방해하지 않는 3px 핀포인트 나노 규격 유지.
  - **차트 툴팁 (`plugins.tooltip`)**: `🔴 매수 진입 (Buy)`, `🔵 익절 체결 (TP)`, `⭕ 손절 청산 (SL)`, `▲ 트레일링 청산 (TS)`, `○ 타임컷 청산 (TC)` 표시.

### 17. 줌인/줌아웃 연동 동적 아이콘 스케일링, 투자금 100만 기본화 & AI 실시간 눌림목 타점 예측(Live Forecast) HUD 구축 (2026-08-16)
- **줌 레벨 연동 동적 포인트 스케일링 (`updateDynamicMarkerScale`)**:
  - 휠 줌, 핀치 줌, 좌우 패닝, 툴바 버튼 조작 시 현재 화면에 노출되는 캔들 수(`visibleBars`)를 실시간 계산하여 신호 아이콘의 크기를 비례 조정.
  - **줌인(근접 확대, $\le 22$일)**: 5.6px ~ 8.5px의 큼직하고 선명한 High-DPI 포인트로 확대되어 캔들과의 정밀한 타점 위치 확인 용이.
  - **줌아웃(원거리 축소, $> 180$일)**: 1.1px ~ 1.8px의 초소형 핀포인트로 축소되어 차트가 마커로 가려지거나 복잡해지는 현상을 원천 방지.
- **투자금 기본값 및 퀵 칩 개편**:
  - 기본 투자금: `1,000,000원 (100만원)`으로 재설정.
  - 원클릭 퀵 칩: `100만` / `500만` / `1천만` / `3천만` / `5천만` 구성.
### 22. 마우스 포인터 오버 지점 기준 점진적 알약 채움 애니메이션 전면 구현 (2026-08-16)
- **커서 좌표 감지 기반 방사형 채움 효과 (Pointer-Origin Radial Fill Animation)**:
  - 사용자가 알약 버튼(투자금 퀵 칩, 차트 범례 필터, 모달 필터 탭, 차트 툴바 등) 위로 마우스를 올리거나 클릭할 때, **마우스 포인터가 진입한 정확한 $(x, y)$ 지점을 중심점(`--cursor-x`, `--cursor-y`)으로 하여 점진적으로 원형 확장되며 버튼 전체를 채우는 럭셔리 마이크로 인터랙션** 구현.
- **적용 대상 컴포넌트**:
  - **투자금 퀵 칩 (`.btn-cap-chip`)**: 코발트 블루 리플 및 클릭 시 100% 솔리드 채움.
  - **차트 범례 신호 필터 (`.legend-filter-btn`)**: `ALL`, `BUY(로즈)`, `TP(코발트)`, `SL(로즈)`, `TS(에메랄드)`, `TC(코발트)` 각 신호 고유 컬러 그라데이션 방사형 확장.
  - **모달 거래 필터 탭 (`.tab-filter`)**: 코발트 방사형 채움.
  - **차트 툴바 컨트롤 (`.btn-chart-ctrl`, `.btn-equity-pill`)**: 에메랄드 및 잉크 네이비 방사형 확장.
  - **시뮬레이션 실행 버튼 (`.btn-primary`)**: 고휘도 화이트 글로우 웨이브 확장.
  - **전략 프리셋 타일 & 설정 기어 버튼 (`.btn-preset`, `.btn-gear-circle`, `.recent-chip`)**.
### 23. AI 추천 전략·실시간 진단 알약 디자인 일원화 & 선택/활성 시 고대비 폰트 컬러 전면 개선 (2026-08-16)
- **전략 및 실시간 진단 영역 알약 디자인 일원화**:
  - AI 추천 전략 카드의 조건 태그(`.preset-tag`), 핵심 KPI 알약(`.kpi-pill`, `.pill-sub`), 실시간 진단 카드의 확률 뱃지(`.forecast-prob-badge`, `.prob-level`), 4대 타겟 알약(`.kpi-pill`, `.pill-sub`) 모두 **마우스 진입점 기준 점진적 방사형 채움(Pointer-Origin Radial Fill)** 애니메이션 파이프라인에 통합.
- **선택/활성 시 폰트 컬러 시인성 및 대비도 극대화**:
  - **전략 타일 활성화 (`.btn-preset.active`)**:
    - 전략 설명문(`.preset-desc`): `#334155` -> `#1E293B` 굵은 폰트로 선명도 강화.
    - 조건 태그(`.preset-tag`): 흰색 고대비 알약 배경에 `#090E24` 딥 블랙 폰트(`font-weight: 800`) 및 섬세한 테두리 그림자 적용.
    - 전략 뱃지(`.preset-badge`): 각 전략별 고유 테마 컬러(인디고, 에메랄드, 사이언, 앰버) 솔리드 배경 + 순백색 텍스트(`color: #FFFFFF`)로 전환하여 가독성 100% 확보.
  - **신호 범례 필터 활성화 (`.legend-filter-btn.active`)**:
    - `BUY(로즈)`: `#9F1239` 딥 버건디 폰트 + `#E11D48` 솔리드 카운트 뱃지.
    - `TP(코발트)`: `#0369A1` 딥 오션 폰트 + `#0284C7` 솔리드 카운트 뱃지.
    - `SL(로즈)`: `#9F1239` 딥 버건디 폰트 + `#E11D48` 솔리드 카운트 뱃지.
    - `TS(트레일링)`: `#065F46` 딥 에메랄드 폰트 + `#059669` 솔리드 카운트 뱃지.
    - `TC(타임컷)`: `#1E40AF` 딥 블루 폰트 + `#2A3FF5` 솔리드 카운트 뱃지.
    - `ALL`: `#090E24` 잉크 블랙 배경 + `#FFFFFF` 순백색 텍스트.
### 24. 알약 버튼 선택 시 텍스트 가림 버그(z-index 스태킹) 해결 & 원화(₩) 기호 폰트 겹침 및 아이콘 정상화 (2026-08-16)
- **알약 텍스트 가림 원인 분석 및 해결**:
  - **원인**: 알약 버튼 내부의 순수 텍스트 노드(Bare Text Node, 예: `5천만`, `매수 (Buy)`)가 HTML 태그로 감싸지지 않아 `.btn-cap-chip > *`의 `z-index: 2`를 부여받지 못하고, `::before` 가상 요소(`z-index: 1`)의 솔리드/그라데이션 배경 아래(Layer 0)로 묻혀서 글자가 통째로 사라지던 현상 발생.
  - **해결**:
    1. 모든 알약 버튼 텍스트를 `<span>` 및 `<span class="legend-text">`로 명시적 래핑.
    2. `::before` 가상 요소의 `z-index`를 `0`으로 하향하고, 내부 모든 텍스트/아이콘 레이어에 `position: relative; z-index: 2` 및 `color: inherit`을 부여하여 활성화/호버 시에도 텍스트가 항상 배경 위에 100% 선명하게 렌더링되도록 수정.
- **원화(₩) 기호 폰트 겹침 현상 수정**:
### 25. Leonardo.ai 시그니처 버튼 인터랙션(Shimmer Sweep, 탄성 압축, 네온 펄스) 전면 구현 (2026-08-16)
- **Leonardo.ai 스타일 4단계 CTA 인터랙션 엔진 구축**:
  1. **호버 시 대각선 광원 스윕 (Hover Shimmer Light Sweep)**:
     - 마우스 호버 시 25도 회전된 55% 투명도의 대각선 광원 띠(`::after`)가 버튼 표면을 650ms 동안 매끄럽게 훑고 지나가는 럭셔리 쉬머 애니메이션 구현.
  2. **마그네틱 리프트 & 앰비언트 네온 글로우 (Magnetic Hover Aura)**:
     - 호버 시 `translateY(-2px) scale(1.015)`로 미세하게 떠오르며 다크 블루/인디고 네온 글로우(`box-shadow: 0 8px 22px rgba(42, 63, 245, 0.50), 0 0 16px rgba(99, 102, 241, 0.40)`)가 주변으로 은은하게 발산.
  3. **클릭 시 택타일 탄성 압축 (Elastic Click Compression)**:
     - 버튼 클릭 및 탭 시 `transform: translateY(1px) scale(0.96)`의 즉각적인 80ms 물리적 햅틱 압축 피드백 제공.
  4. **클릭 시 네온 펄스 충격파 방출 (Click Shockwave Halo Pulse)**:
     - 클릭 순간 버튼 둘레로 500ms 동안 반경 12px의 원형 에너지 펄스 충격파(`leonardoShockwave` 키프레임)가 확 퍼져나가는 고유 인터랙션 완성.
### 26. Shopify Polaris 디자인 시스템(DESIGN-shopify.md) 전면 적용 (2026-08-16)
- **Shopify 듀얼 트랙 디자인 규격 완벽 이식**:
  1. **Canvas Polarity & Palette**:
     - `canvas-cream` (`#fbfbf5`) 따뜻한 오프화이트 에디토리얼 배경 및 `canvas-light` (`#ffffff`) 화이트 패널.
     - `aloe-10` (`#c1fbd4`) 성장/피처드 민트 및 `pistachio-10` (`#d4f9e0`) 소프트 피스타치오 악센트 도입.
     - `shade-30` ~ `shade-70` 정밀 그레이 스케일 및 `hairline-light` (`#e4e4e7`) 1px 크리스프 디바이더.
  2. **Typographic DNA**:
     - `Neue Haas Grotesk Display` / `Inter Display`의 330/500 슬림 웨이트 헤드라인 및 `Inter Variable` (420/550) 바디.
     - 전역 `font-feature-settings: "ss03" 1, "cv02" 1, "cv03" 1, "cv04" 1;` 적용.
  3. **Non-Negotiable Pill Architecture**:
     - 모든 버튼, 검색 필터, 범례 칩, 투자금 칩, 태그를 `9999px` 알약(`border-radius: var(--radius-pill)`) 형태로 통일.
     - Shopify 대표 컴포넌트(`button-primary-pill`, `button-outline-on-light`, `button-aloe-pill`, `pill-tag-mint`, `card-pricing-featured`) 구현.
  4. **Level 3 Stacked Tiny Shadows (Paper Halo)**:
     - `box-shadow: 0 8px 8px rgba(0,0,0,0.03), 0 4px 4px rgba(0,0,0,0.03), 0 2px 2px rgba(0,0,0,0.03), 0 0 0 1px #e4e4e7;`를 모든 카드에 적용하여 부드럽고 고급스러운 페이퍼 질감 완성.
### 27. Voltagent 다크 & 일렉트릭 그린 디자인 시스템(DESIGN-voltagent.md) 전면 적용 (2026-08-16)
- **Voltagent 개발자 엔지니어링 플랫폼 디자인 언어 전면 구현**:
  1. **Near-Black Canvas (`#101010`) & Single Electric Green (`#00D992`)**:
     - 타협 없는 풀다크 캔버스(`--canvas: #101010`, `--canvas-soft: #1A1A1A`, `--canvas-card: #141414`) 구축.
     - 단일 시그니처 일렉트릭 그린(`--primary: #00D992`, `--primary-soft: #2FD6A1`)을 CTA, 라이브 인디케이터, 번개 로고에 집중 배치.
  2. **Hairline Elevation System (`#3D3A39`)**:
     - 머티리얼 드롭 섀도우를 배제하고 1px 솔리드 헤어라인 보더(`--hairline: #3D3A39`)와 대시 구분선(`1px dashed rgba(79, 93, 117, 0.4)`)으로 엔지니어링 미학 완성.
  3. **Inter & SF Mono / IBM Plex Mono 타이포그래피**:
     - `Inter` (Display/Body 400/600, OpenType calt/rlig)와 `IBM Plex Mono` (수치/코드 400/600) 조합.
     - Uppercase Eyebrow 트래킹 `2.52px` 적용.
  4. **컴포넌트 지오메트리 (6px / 8px Radii)**:
     - 버튼과 인풋은 `6px` (`--radius-sm`), 카드/차트 컨테이너는 `8px` (`--radius-md`), 라이브 상태 태그만 `9999px` 알약 적용.
     - `button-primary` (일렉트릭 그린 CTA) + Leonardo.ai 쉬머 스윕 & 충격파 펄스 연동.
  5. **Chart.js 다크 & 네온 시각화 동기화**:
     - 종가선(순백색 `#FFFFFF`), 20일선(일렉트릭 그린 `#00D992`), 60일선(시안 `#38BDF8`), AI 예측(일렉트릭 그린 반투명 채움), 다크 툴팁(`rgba(26,26,26,0.96)`) 전면 동기화.

### 29. 분석 기간 기본값 최근 1년(250일) 설정 & 투자금 기본값 현재 주가 기준 10배 동적 연동 (2026-08-16)
- **분석 기간 기본값 최적화**:
  - `<select id="timeframe">`의 기본값을 기존 500일에서 **`250일(최근 1년)`**로 기본 지정.
- **종목별 현재 주가 기준 10배(10주분) 동적 연동**:
  - 첫 로딩 및 신규 종목 검색/선택 시, 해당 종목의 최신 종가(`lastClose`)에 기반하여 **현재 주가의 약 10배(`Math.round(lastClose * 10)`)** 금액을 투자금 인풋(`initialCapitalInput`)에 자동으로 세팅.
  - 퀵 칩에 **`10배 (기본)`** 옵션을 추가하여 언제든 현재 종목 주가 $\times$ 10배 금액으로 즉시 환원할 수 있도록 지원.

### 31. 고성능 벡터 봉차트(Candlestick) 렌더러 구축 & 봉/라인 인터랙티브 토글 구현 (2026-08-16)
- **고성능 커스텀 캔들스틱(봉차트) 엔진 탑재 (`candlestickPlugin`)**:
  - KRX 주식 시장 표준 규격에 맞추어 **양봉(상승: `#EF4444` Rose Red)**과 **음봉(하락: `#38BDF8` Cyan Blue)** 캔들스틱 벡터 렌더러 구현.
  - 마우스 휠 줌/드래그 패닝 레벨에 연동되어 캔들 폭(2.0px~24px) 및 심지(Wick) 두께가 실시간으로 동적 스케일링.
- **툴바 봉차트 / 라인차트 원클릭 토글 (`#chartTypeCandleBtn`, `#chartTypeLineBtn`)**:
  - 기본 차트 모드를 **`봉차트 (Candlestick)`**로 설정하고, 원클릭으로 전통 라인차트와 전환 가능.
- **상세 시고저종(OHLC) 툴팁 인스펙터**:
  - 마우스 오버 시 `시가(Open)`, `고가(High)`, `저가(Low)`, `종가(Close)`, `등락률(%)`, `🔴 양봉 / 🔵 음봉` 판별 정보 제공.

### 32. 차트 내 매매 타점 마커(Dot) 테두리 일렉트릭 그린(#00D992) 야광 네온 링 전면 적용 (2026-08-16)
- **사이트 시그니처 Electric Green (`#00D992`) 야광 네온 테두리 적용**:
  - `BUY` (매수), `TP` (익절), `SL` (손절), `TS` (트레일링), `TC` (타임컷) 등 차트 상의 모든 매매 타점 마커의 외곽 테두리를 사이트의 일렉트릭 그린(`#00D992`) 형광 네온 컬러로 일괄 적용 (`pointBorderWidth: 2.2px`).
  - 캔들스틱(양봉/음봉) 및 다크 캔버스 배경 위에서 마커가 선명하게 발광하는 야광 네온 효과를 구현하여 타점 시인성 극대화.
  - 상단 범례(Legend)의 컬러 뱃지 및 인디케이터에도 동일한 일렉트릭 그린 네온 테두리 및 글로우 효과 동기화.

### 33. 봉차트-타점 마커 간 시각적 분리 최적화 (2026-08-16)
- **봉차트 반투명(Translucent) 깊이감 처리**:
  - 양봉(`rgba(239, 68, 68, 0.40)`)과 음봉(`rgba(56, 189, 248, 0.40)`)의 바디를 40% 반투명 처리하고 1px 외곽 테두리를 주어, 캔들이 타점 마커를 시각적으로 먹어치우지 않고 깨끗한 청사진 배경 역할을 수행하도록 개선.
- **다크 아이솔레이션 쉴드 플러그인 (`markerHaloPlugin`) 탑재**:
  - 캔들과 마커 사이에 `#101010` 다크 분리막과 `rgba(0, 217, 146, 0.85)` 네온 발광 아우라를 3D 레이어로 자동 렌더링하여 마커가 캔들 색상과 관계없이 100% 팝업되도록 격리.
- **5종 매매 타점 고유 기하학 도형 & 형광 컬러 분리**:
  - **매수(BUY)**: 원형(Circle) + 네온 코랄 레드 코어 + 일렉트릭 그린 2.5px 링
  - **익절(TP)**: 다이아몬드(Diamond, `rectRot`) + 초고휘도 네온 그린 코어 + 화이트 2.5px 링
  - **손절(SL)**: 엑스 마크(Cross, `crossRot`) + 비비드 크림슨 레드 + 화이트 3.0px 링
  - **트레일링(TS)**: 상승 삼각(Triangle) + 일렉트릭 바이올렛 코어 + 일렉트릭 그린 2.5px 링
  - **타임컷(TC)**: 정사각(Square, `rect`) + 하이볼테이지 오렌지 코어 + 화이트 2.5px 링
- **상단 범례(Legend) 및 거래 내역 뱃지 아이콘 동기화**:
  - 상단 필터 범례 및 모달 테이블의 신호 아이콘을 동일한 5종 기하학 벡터 모양으로 통일.

### 34. 차트 렌더러 플러그인 런타임 ReferenceError 방어 및 안전성 강화 (2026-08-16)
- **런타임 예외 원인 분석 및 신속 해결**:
  - `markerHaloPlugin` 내에서 `scales` 객체 디스트럭처링 검증 시 로컬 변수 미정의로 인해 `ReferenceError: scales is not defined` 예외가 발생하여 차트 렌더링 루프가 일시 정지되던 현상 원천 해결.
- **안전 방어막(Safety Net) 및 예외 격리 구축**:
  - `candlestickPlugin` 및 `markerHaloPlugin` 내 모든 캔버스 렌더링 블록을 `try-catch-finally`로 완벽 캡슐화하여 렌더링 중 예외가 발생하더라도 차트와 UI 이벤트 루프가 멈추지 않도록 안전성 극대화.

### 35. 서비스 타이틀(NOSTOS) 및 'Jump from the Dip Strategy' 타이틀에 Space Grotesk 폰트 적용 (2026-08-16)
- **Google Fonts `Space Grotesk` 도입 (`index.html`)**:
  - `Space Grotesk` (wght 500, 600, 700, 800) 웹폰트 로드.
- **타이틀 전용 타이포그래피 변수 `--font-title` 정의 (`style.css`)**:
  - `--font-title: 'Space Grotesk', 'Inter', system-ui, sans-serif;` 지정.
- **주요 브랜드 및 헤더 타이틀 적용**:
  - 상단 서비스 타이틀: `.logo-text h1` (`NOSTOS: Buy the Dip`)에 `Space Grotesk` 700 볼드 적용.
  - 올인원 전략 청사진 카드 타이틀: `.jftd-title-wrapper .section-title` (`Jump from the Dip Strategy`)에 `Space Grotesk` 700 볼드 적용.
  - 미래지향적 기하학적 인텔리전스 청사진 미학 완성.

### 36. 종목 검색/변경 시 차트 Y축 가격 범위(Scale) 동적 자동 맞춤(Auto-Fit) 전면 구현 (2026-08-16)
- **종목 전환 및 Y축 스케일 고정 문제 원인 분석 & 해결**:
  - 다른 종목(예: SK하이닉스 17만원, 네이버 18만원, 씨이랩 1만원 등) 검색 시, 거래일 수가 동일한 경우 기존 인플레이스 업데이트 조건에 걸려 Y축 가격 범위 및 주가 데이터셋이 이전 종목 기준으로 멈춰있던 문제 원천 해결.
  - `currentChartSymbol`과 `currentChartDays`를 별도 추적하여 **종목이나 분석 기간이 변경되면 전체 캔들/이동평균/바운드 데이터셋을 완전히 재구성하고 차트를 신규 종목 가격대에 맞게 즉각 재생성**.
- **동적 Y축 여백(Padding) & 가독성 틱 포맷터 탑재**:
  - 선택된 종목의 실제 최저가(`minPrice`)와 최고가/목표가(`maxPrice`)를 실시간 계산하여 상하 8%의 완충 여백(`yPadding`)을 둔 `suggestedMin` / `suggestedMax` 자동 설정.
  - Y축 가격 라벨에 원화 단위(`₩ 175,000`, `₩ 12,300` 등) 3자리 콤마 포맷팅을 적용하여 어떤 가격대의 종목이든 완벽한 차트 구도 완성.

### 37. AI 다방향(상승 반등/하락 이탈/중립 횡보) 실시간 타점 예측 엔진 전면 구축 (2026-08-16)
- **일방적 상승 예측 문제점 개선**:
  - 기존 예측선이 종목의 추세나 하락 리스크와 무관하게 항상 익절가(+TP%)로 상승하도록 고정되어 있던 한계를 극복.
- **실전 퀀트 기반 3대 시장 시나리오 평가 엔진 (`evaluateMarketForecast`) 탑재**:
  - **🟢 상승 반등 시나리오 (`BULLISH`)**: 60일선 지지, 20/60일선 정배열 골든크로스, 건전한 눌림(-3%~-10%), RSI 32~46 안정권 유지 시 도약 반등 목표가(+TP%) 및 일렉트릭 그린(`#00D992`) 상승 곡선 투사.
  - **🔴 하락 이탈/지지 붕괴 시나리오 (`BEARISH`)**: 20일선 및 60일선 동시 하향 이탈, 데드크로스 역배열, 과매수 고점 이탈(RSI 70+) 또는 지지선 붕괴 시 하방 위험가(-4.5%~-8%) 및 비비드 레드(`#EF4444`) 하향 곡선 투사 + 급락 경보 알림.
  - **🟡 중립 횡보/조정 시나리오 (`NEUTRAL`)**: 방향성 탐색 박스권 시 앰버(`#FB923C`) 횡보 지지선 테스트 곡선 투사.
- **실시간 HUD 청사진 및 툴팁 완벽 동기화**:
  - 'Jump from the Dip Strategy' 카드의 알약 라벨/아이콘/색상(`목표 도약가` vs `하방 경보 목표가`, `text-cobalt` vs `text-rose`) 및 종합 진단 해설이 상승/하락 상태에 맞춰 100% 동적으로 변경되도록 연동.

### 38. Jump Strategy 4대 타일 실시간 액션 가이드(Actionable Trigger) 마이크로 뱃지 전면 탑재 (2026-08-17)
- **정보(Info)에서 실행(Action)으로의 UX 고도화**:
  - 트레이더가 가격 수치만 보고 겪는 결정 피로(Decision Friction)를 원천 차단하기 위해, 4개 타일(매수 지지선, 목표 도약가, 손절 방어선, 예상 보유 기간) 하단에 **구체적인 매매 실행 지침(Actionable Trigger)** 마이크로 뱃지 탑재.
- **AI 시장 시나리오(상승/하락/횡보) 연동 4대 실행 수칙**:
  - **1. 매수 지지선**:
    - `BULLISH`: `🎯 1차 40% 분할 매수 대기`
    - `BEARISH`: `⛔ 신규 매수 금지 / 하방 관망`
    - `NEUTRAL`: `🔍 박스권 하단 지지 확인 후 진입`
  - **2. 목표 도약가 / 하방 경보가**:
    - `BULLISH`: `💰 도달 시 50% 분할 익절`
    - `BEARISH`: `🔻 하방 이탈 시 잔여분 비중 축소`
    - `NEUTRAL`: `⚡ 단기 저항선 부근 분할 매도`
  - **3. 손절 방어선**:
    - `BULLISH`: `🛑 종가 이탈 시 기계적 손절`
    - `BEARISH`: `🚨 붕괴 즉시 전량 비상 탈출`
    - `NEUTRAL`: `🛡️ 박스 하단 이탈 시 원칙 청산`
  - **4. 예상 보유 기간**:
    - `BULLISH`: `🚀 목표 도달 시 잔여분 트레일링`
    - `BEARISH`: `⏳ 하락 추세 진정 시까지 대기`
    - `NEUTRAL`: `⌛ 기간 내 미상승 시 타임컷`
- **Voltagent 다크 미학 스타일링 (`style.css`)**:
  - `.pill-action-guide` 컴포넌트 추가 및 11.5px 크리스프 타이포그래피, 상승(`.action-bullish`, 초록 뱃지), 하락(`.action-bearish`, 레드 뱃지), 횡보(`.action-neutral`, 주황 뱃지) 컬러 코딩 완성.

### 39. AI 다차원 정밀 전술 진단 엔진 (`generateTacticalCommentary`) 구축 & 반복 문구 원천 해결 (2026-08-17)
- **종목별 템플릿 반복 문제 원인 분석 & 해결**:
  - 기존에는 3개 시나리오(상승/하락/횡보)별로 1~2개의 고정 템플릿 문장에 종목명만 치환하여 출력했기 때문에, 여러 종목 검색 시 동일한 문장이 반복되어 전문성이 떨어져 보이는 문제가 발생함.
- **다차원 실시간 기술 지표 결합 3단 전술 보고서 생성기 구축**:
  - **1단 (이평선 정배열/역배열 및 이격도)**:
    - 20일선 이격도(`diff20Pct`), 60일선 이격도(`diff60Pct`), 5영업일 주가 변동률(`momentum5d`), 골든/데드크로스 여부를 계산하여 종목별 고유한 추세 및 지지/저항 국면 서술.
  - **2단 (거래량 배수 & RSI 모멘텀 & 20일 고점 낙폭)**:
    - 20일 평균 거래량 대비 최근 거래량 배수(`volRatio`), RSI 과매도/눌림목/과열 구간 판별, 최근 20일 최고가(`recentHigh`) 대비 실제 낙폭(`dropFromHigh`)을 결합하여 정밀 진단.
  - **3단 (과거 1년 백테스트 승률 & 구체적 분할 매매 실행 수칙)**:
    - 해당 종목의 과거 1년 실증 백테스트 승률(`winRate%`), 성공 횟수(`winCount/trades.length`), 평균 손익률(`avgReturn%`)과 도약 목표가(`targetPrice`), 지지선(`expectedEntry`), 손절선(`stopLoss`)을 1차 분할 매수 비중(40%), 목표가 분할 익절(50%), 기계적 손절 수칙과 결합하여 완벽한 실행 로드맵 제시.
- **결과**:
  - 현대차, SK하이닉스, 삼성전자, 씨이랩 등 어떤 종목을 검색하더라도 **해당 종목의 실제 주가, 지표, 거래량, 과거 백테스트 팩트에 기반한 100% 고유한 전문 리포트가 생성**됨.

### 40. '예상 매수가' 명칭 개편 & 아이콘-라벨 일체형 단일행(Single-Line) 헤더 레이아웃 완성 (2026-08-17)
- **라벨 텍스트 직관화**:
  - 기존 '예상 매수 지지선' $\rightarrow$ **'예상 매수가'**로 간결화. (하락 시나리오에서는 '예상 하방 지지선'으로 자동 연동).
- **타일 내부 3단 수직 아키텍처 완성**:
  - **1행 (헤더)**: 좌측에 `[아이콘 + 라벨]`을 가로 한 줄(`pill-title-group`)로 밀착 정렬하고, 우측에 `[20일선 지지]` 등 서브태그(`pill-sub`) 배치.
  - **2행 (가격/수치)**: 19px 대형 모노스페이스 가격 수치(`pill-val-row`) 굵게 출력.
  - **3행 (액션 가이드)**: 하단에 마이크로 실행 뱃지(`pill-action-guide`) 배치.
- **적용 결과**:
  - 4대 타일 전체가 동일한 황금비율 레이아웃(헤더 $\rightarrow$ 가격 $\rightarrow$ 실행지침)을 갖추어 시각적 통일감 및 가독성이 크게 향상됨.

### 41. Jump Strategy 4대 타일 2-컬럼 구조 개편 (좌측: 3행 정보·액션 / 우측: 대형 가격 수치) (2026-08-17)
- **사용자 지정 2-컬럼 그리드 아키텍처 구현**:
  - **첫 번째 컬럼 (좌측 `pill-col-left`)**:
    - **1행**: `[아이콘 + 예상 매수가 / 목표 매도가 / 손절 방어선 / 예상 보유 기간]`
    - **2행**: `[20일선 지지 / +6.0% / -3.0% / 단기 반등 유효]` 서브태그
    - **3행**: `[🎯 1차 40% 분할 매수 대기 / 💰 도달 시 50% 분할 익절 / 🛑 종가 이탈 시 기계적 손절 / 🚀 목표 도달 시 잔여분 트레일링]` 액션 가이드 마이크로 뱃지
  - **두 번째 컬럼 (우측 `pill-col-right`)**:
    - `₩ 397,300` 등 20px 볼드 모노스페이스 대형 가격 수치를 우측에 집중 배치하여 시선 분산 없이 가격과 행동 지침이 직관적으로 한눈에 매칭되도록 완성.

### 42. 실행 전략 4대 프리셋 네이밍 전면 개편 (2026-08-17)
- **전략 프리셋 명칭 직관화 및 표준화**:
  - `기본 균형 도약` $\rightarrow$ **`기본 매수`** (익절 6% / 손절 3% + 7일 컷 & TS 3%)
  - `트레일링 추세 도약` $\rightarrow$ **`대세 추종 매수`** (익절 12% / 손절 3.5% + TS 2.5%)
  - `단기 타임컷 도약` $\rightarrow$ **`타임컷(4일 미상승 청산)`** (익절 4% / 손절 2% + 4일 컷)
  - `고손익비 스윙 도약` $\rightarrow$ **`120일선 스윙`** (익절 8% / 손절 2.5% + 10일 스윙)
- **HTML 마크업 및 JS 매핑 객체(`PRESETS`) 일괄 동기화**:
  - 버튼 타이틀, 설명 문구, JS 런타임 프리셋 객체(`PRESETS[id].name`)를 완전 통일.

### 43. LIVE AI BLUEPRINT 뱃지 제거, 상세설정 톱니바퀴 버튼 재배치 & '눌림목 매수 전략' 헤더 개편 (2026-08-17)
- **메인 카드 헤더 간결화**:
  - `LIVE AI BLUEPRINT` 펄스 뱃지를 제거하여 상단 타이틀 영역의 시각적 복잡도를 최소화하고 깔끔한 헤더 완성.
- **상세설정 톱니바퀴 버튼(`toggleCustomSettingsBtn`) 동선 일원화**:
  - 기존 상단 헤더 우측에 있던 톱니바퀴 버튼을 전략 프리셋 영역 헤더(`.jftd-sub-header`) 우측으로 이동.
  - 사용자가 **'눌림목 매수 전략'** 목록을 보면서 바로 우측의 톱니바퀴를 눌러 커스텀 수치를 조절할 수 있도록 기능적 맥락을 완벽히 일치시킴.
- **헤더 명칭 개편**:
  - `도약 실행 전략 선택 (Jump Strategy Presets)` $\rightarrow$ **`눌림목 매수 전략`**으로 명칭을 직관화.

### 44. 상세설정 내 '수정된 매수 전략 적용' 버튼 탑재 & 차트 상단 20일선(초록)/60일선(파랑) 범례 뱃지 추가 (2026-08-17)
- **'수정된 매수 전략 적용' (`#btnApplyCustomStrategy`) 버튼 구현**:
  - 상세 설정 드로어 하단에 일렉트릭 그린 테마의 직관적인 CTA 버튼을 배치.
  - 사용자가 익절률, 손절률, 추세 필터, RSI, 트레일링, 타임컷 최대보유 일수 등을 조절한 후 버튼을 누르면 즉각 시뮬레이션(`runSimulation()`)이 실행되며 촉각적 피드백('적용 완료' 체크마크)을 제공.
- **차트 이동평균선(MA) 시각적 범례 추가**:
  - 🟢 **초록색 실선 (`#00D992`)**: **20일 이동평균선 (20-day SMA)** $\rightarrow$ 단기 추세 및 핵심 눌림목 지지선.
  - 🔵 **파란색/시안 실선 (`#38BDF8`)**: **60일 이동평균선 (60-day SMA)** $\rightarrow$ 중기 추세선 및 대세 우상향 판별선.
  - 차트 상단 범례 우측에 `[20일선]` / `[60일선]` 가이드 뱃지를 영구 표시하여 사용자가 한눈에 식별할 수 있도록 가시성 완성.

### 45. 상세설정 드로어 2-로우(1행: 텍스트·선택 5종 / 2행: 슬라이더 3종) & 우측 '전략 수정' 알약 버튼 완성 (2026-08-17)
- **컨트롤 유형별 2-로우 수평 레이아웃 구축**:
  - **좌측 2단 컨트롤 컨테이너 (`.custom-rows-container`)**:
    - **1행 (텍스트/선택 입력 5종 `.custom-row-inputs`)**: `[목표 익절률 (%)]`, `[손절 한도 (%)]`, `[추세 필터 (60/120/0일선)]`, `[눌림목 알고리즘 (이평선+RSI/볼린저/낙폭)]`, `[슬리피지 & 수수료 (%)]`
    - **2행 (슬라이드 컨트롤 3종 `.custom-row-sliders`)**: `[RSI 임계값 (45)]`, `[트레일링 하락폭 (3.0%)]`, `[타임컷 최대보유 (7일)]`
  - **우측 액션 컬럼 (`.custom-action-col`)**:
    - **9999px 알약 버튼 (`.btn-pill-apply`)**: **`[▶ 전략 수정]`** (Electric Green 테마, 셰이머 광원, 햅틱 클릭 피드백).
- **반응형 대응**:
  - 900px 이하 뷰포트에서는 세로 스택 모드로 자동 전환되어 모바일/태블릿에서도 조작 편의성 보장.

### 46. 20일선(주황)/60일선(초록) 차트 색상 개편 & Custom 전략 상세설정 상시 접힘 아코디언 분리 (2026-08-17)
- **차트 이동평균선 색상 직관화 개편**:
  - 🟠 **20일선 (SMA20)**: 기존 초록색 $\rightarrow$ **주황색 (Orange `#FB923C`)** 실선 및 범례 뱃지 변경.
  - 🟢 **60일선 (SMA60)**: 기존 파란색 $\rightarrow$ **초록색 (Electric Green `#00D992`)** 실선 및 범례 뱃지 변경.
- **Custom 전략 상세설정 전용 아코디언 분리 (`custom-strategy-accordion-wrap`)**:
  - 기존 4대 전략 프리셋 하단에 독립된 **`Custom 전략 상세 설정 [직접 조정]`** 아코디언 토글 바 배치.
  - **항상 기본 접힘(Collapsed) 상태를 유지**: 일반 프리셋 버튼 클릭 시 드로어가 열리지 않고 항상 깔끔하게 접힌 상태가 유지됨.
  - 토글 클릭 시에만 부드럽게 펼쳐지며, 2-로우(텍스트 5종 + 슬라이더 3종) 입력창 및 우측의 `[▶ 전략 수정]` 9999px 알약 버튼을 통해 즉각 커스텀 파라미터 백테스트 실행 가능.

### 47. Custom 상세설정 제거 & 전략 백테스트 성과 지표(Part 3) 가로 너비 상단 요소와 100% 일원화 (2026-08-17)
- **Custom 설정 UI 전면 정리**:
  - 사용자 요구사항에 맞춰 Custom 아코디언 드로어 및 톱니바퀴 조작부를 전면 제거하고, 4대 표준 프리셋(`기본 매수`, `대세 추종 매수`, `타임컷`, `120일선 스윙`) 중심으로 UI를 극대화.
- **성과 지표(Part 3 KPI 알약 바) 가로 너비 완전 일원화**:
  - 메인 카드(`jftd-strategy-card`) 내부 태그 구조를 완벽히 정렬하여 **1단(도약 타점 예측 4개 알약 + AI 진단 리포트) $\rightarrow$ 2단(눌림목 매수 전략 4대 프리셋 카드) $\rightarrow$ 3단(전략 백테스트 성과 지표 4개 KPI 알약)**이 모두 동일한 너비와 패딩 그리드로 100% 깔끔하게 일치되도록 완성.

### 48. 20일선(주황 실선)/60일선(초록 실선) 전면 적용 & 휠/버튼 줌인·줌아웃 속도 감속 최적화 (2026-08-17)
- **차트 이동평균선(MA) 주황/초록 실선(Solid Line) 렌더링**:
  - 🟠 **20일선 (SMA20)**: **주황색 실선 (`#FB923C`, Solid Line)** $\rightarrow$ 1.5px 굵기의 매끄러운 단기 눌림목 지지선.
  - 🟢 **60일선 (SMA60)**: **초록색 실선 (`#00D992`, Solid Line)** $\rightarrow$ 1.5px 굵기의 일렉트릭 그린 중기 추세선.
- **마우스 휠 줌(Wheel Zoom) 및 툴바 줌 버튼 속도 미세 감속(Smooth Deceleration)**:
  - 마우스 휠 줌 스피드를 `0.002`로 정밀 감속하여 트랙패드/마우스 휠 스크롤 시 급격한 화면 튐 없이 부드럽고 섬세한 캔들 확대를 구현.
  - 상단 툴바 줌 버튼(`+`, `-`)의 배율을 `1.08x` / `0.92x`의 완만한 단계로 최적화.

### 49. 120일선(보라 실선) & 5일선 탑재 및 4대 이동평균선(5/20/60/120일) 원클릭 토글 인터페이스 완성 (2026-08-17)
- **120일선 스윙 전략을 위한 120일선(SMA120) & 5일선(SMA5) 전면 구축**:
  - 🟣 **120일선 (SMA120)**: **보라색 실선 (`#A855F7`, Solid Line, 1.5px)** $\rightarrow$ 장기 경기/스윙 핵심 지지선. `120일선 스윙` 프리셋 선택 시 차트상에 자동 하이라이트 및 활성화.
  - 🟢 **60일선 (SMA60)**: **초록색 실선 (`#00D992`, Solid Line, 1.5px)** $\rightarrow$ 중기 수급선.
  - 🟠 **20일선 (SMA20)**: **주황색 실선 (`#FB923C`, Solid Line, 1.5px)** $\rightarrow$ 단기 눌림목 지지선.
  - ⚪ **5일선 (SMA5)**: **실버 그레이 실선 (`#94A3B8`, Solid Line, 1.2px)** $\rightarrow$ 초단기 모멘텀선.
- **차트 상단 4대 이평선 원클릭 개별 토글 바 (`.legend-ma-group`) 구축**:
  - `[— 5일선]`, `[— 20일선]`, `[— 60일선]`, `[— 120일선]` 버튼을 클릭하여 사용자가 원하는 이평선만 0ms 지연 없이 즉각 켜고 끌 수 있는 인터랙티브 토글 완성.

### 50. 차트 휠 줌(Zoom) `Ctrl + 마우스 휠` 키 조합 전면 적용 (2026-08-17)
- **`Ctrl + Mouse Wheel` 줌인/줌아웃 키 조합 구축**:
  - `chartjs-plugin-zoom` 옵션에 `modifierKey: 'ctrl'`을 적용하여, 사용자가 **`Ctrl` 키를 누른 채로 마우스 휠을 굴릴 때만** 차트가 확대/축소되도록 제어.
  - 일반 마우스 휠 스크롤 시에는 대시보드 웹페이지가 자연스럽게 상하로 스크롤되어, 차트 영역 위에서 의도치 않게 캔들이 줌인되는 오작동을 원천 방지.
  - 상단 툴바 줌 버튼 툴팁에 `(Ctrl + 휠)` 안내 문구를 추가하여 접근성 확보.

### 51. 차트 기본 뷰 기간 6개월(125 거래일) 전면 적용 (2026-08-17)
- **초기 차트 시야 범위 최적화 (`defaultVisibleDays = 125`)**:
  - 기존 45일(약 2개월)에서 **최근 6개월(약 125 거래일)**로 기본 뷰 윈도우를 확장.
  - 종목 조회 및 차트 초기화 시 6개월간의 단기/중기 추세와 120일선 스윙 지지선 흐름을 한 화면에서 즉시 파악할 수 있도록 가시성 완성.
  - 전체 백테스트 데이터(1년치 250일)는 배경에 완벽히 로드되어 있으므로 좌우 패닝(드래그) 및 줌 조작으로 언제든 과거 데이터를 탐색 가능.

### 52. 누적 자산 성장 곡선 모달 현재 검색 종목 및 전략 실시간 완전 연동 (2026-08-17)
- **현재 검색 종목 및 전략 실시간 동기화 (`latestEquityData` & `renderEquityModalChart`)**:
  - 모달 헤더에 현재 분석 중인 종목명과 티커 뱃지(`#modalStockBadge`, 예: `현대차 (005380.KS)`)를 실시간 표시.
  - 상단 KPI 요약 바를 4열 그리드로 개편하여 **초기 투자금 / 전략 최종 자산(수익률) / 단순 보유 잔고(수익률) / 초과 수익(알파)**을 한눈에 비교.
  - 모달 팝업이 열릴 때(`openEquityModal`)와 종목/전략 변경 시 캔버스 렌더러가 현재 종목의 일별 시세와 백테스트 잔고 곡선을 0ms 즉시 정확한 캔버스 크기로 반응형 렌더링.
  - 툴팁 마우스 오버 시 날짜별 전략 잔고, 단순 보유 잔고, 누적 수익률을 원화 및 퍼센트로 완벽 포맷팅.

### 54. GitHub Pages 웹사이트 배포(Publish) 완료 (`https://drbrookskim.github.io/buythedip/`) (2026-08-17)
- **GitHub Pages 공식 웹 퍼블리싱 활성화**:
  - 배포 URL: `https://drbrookskim.github.io/buythedip/` (HTTP 200 OK 확인 완료)
  - 정적 웹 환경에서도 원활하게 동작하도록 `.nojekyll` 설정 및 `generateRealisticFallbackData` 시뮬레이션 폴백 엔진 탑재.
  - 별도의 서버 구동 없이도 브라우저만으로 2,800+ KRX 종목 탐색, 4대 프리셋 전략 검증, 4종 이평선 토글, 누적 자산 성장 곡선 모달을 누구나 즉시 체험 가능.

### 57. 초고속 실시간 KRX 시세 연동 엔진(Naver+Yahoo) 구축 및 백테스트 전면 실데이터화 (2026-08-17)
- **실시간 주가 데이터 파이프라인 고도화**:
  - `server.py` 내 Naver Finance 고속 XML 파서(`fetch_korean_stock_naver`)와 Yahoo Finance를 하이브리드로 결합.
  - KOSPI/KOSDAQ 2,800+ 전 종목에 대해 50ms 미만의 초고속으로 실시간 250일 일별 시고저종(OHLC) 시세를 공급.
  - 프론트엔드(`app.js`)는 수신된 실데이터를 바탕으로 5종 매매 타점(매수, 익절, 손절, 트레일링, 타임컷), 이동평균선(5/20/60/120MA), AI 도약 목표가 및 복리 누적 자산 성장 곡선(Alpha)을 100% 실제 시장 데이터 기준으로 산출 및 분석.

---

## 📝 2026-08-09 주요 작업 내역

### 1. 눌림목 매도(Exit) 전략 6가지 연구 및 가이드 작성
- **고정 손익비**: 목표 익절(TP) / 손절(SL) 비율 지정.
- **동적 트레일링 스탑**: 진입 후 최고가 대비 N% 하락 시(ATR 기반) 청산하여 상승 추세 추종.
- **저항선 & 이평선 청산**: 전고점 저항선 및 20일/60일선 하향 이탈 시 청산.
- **타임컷 (Time Cut)**: N일 이내 목표 수익 미달 시 기회비용 절감 청산.
- **기술적 지표 과매수 청산**: RSI > 70, Stochastic 과매수 시 청산.
- **분할 익절 + 잔여분 트레일링**: 1차 목표가에서 50% 익절 후 잔여 물량은 트레일링 적용 (권장 조합).

### 2. 하네스(Harness) & 에이전트 팀 시스템 구축
- **CLAUDE.md 포인터 설정**: `dip-strategy-orchestrator` 스킬 연동.
- **전담 에이전트 정의 (`.claude/agents/`)**:
  - `strategy-architect.md`: 매수/매도 전략 및 지표 조합 정형화.
  - `quant-backtester.md`: 수수료/세금/슬리피지 반영 백테스팅 및 지표 평가.
  - `risk-manager.md`: 포지션 사이징, 계좌 손실 한도, MDD 관리.
  - `execution-developer.md`: 증권사 API/알림 봇 자동매매 파이프라인.
- **스킬 팩 (`.claude/skills/`)**:
  - `dip-strategy-orchestrator`: 하네스 전체 조율 및 부분 재실행 관리.
  - `dip-backtest-analyzer`: 백테스팅 리포팅 및 파라미터 감도 분석.

### 3. 실제 주가 연동 웹 시뮬레이터 개발 (`http://localhost:8080`)
- **실제 주가 API 백엔드 (`server.py`)**:
  - `yfinance` 기반 한국 주식(`005930.KS`, `000660.KS`, `035420.KS` 등) 및 미국 주식(`NVDA`, `AAPL`, `TSLA`, `SPY` 등) 시세 데이터 연동.
  - 임의 Ticker 직접 입력 지원.
- **프론트엔드 UI (`index.html`, `style.css`, `app.js`)**:
  - Dark Glassmorphism 테마, 파라미터 조절 슬라이더, 전략 프리셋, KPI 카드, 상세 매매 로그 테이블.
  - 주가 차트 상 매수(삼각형)/매도(원) 신호 매핑 및 자산 성장 곡선 (Equity Curve vs Buy & Hold) 렌더링.

### 4. 차트 일봉 뷰포트 & 조작 인터랙션 고도화
- **기본 2개월 일봉 Viewport**: 백테스트는 장기 데이터(1~5년)로 수행하되, 초기 차트 뷰는 최신 2개월(약 42 영업일) 일봉에 집중하도록 고정.
- **`speed: 0.003` 극초저속 부드러운 휠 줌**: 휠 줌 스피드를 0.003으로 감속 적용하고 `minRange: 15` 및 300ms `easeOutCubic` 이징 애니메이션 추가.
- **좌우 스크롤 패닝 (Horizontal Panning)**:
  - 트랙패드 2핑거 수평 스크롤 및 `Shift + 마우스 휠 스크롤`로 차트 과거/미래 이동 지원.
  - 마우스 좌우 드래그 및 네비게이션 툴바 버튼(`과거`, `미래`, `확대`, `축소`, `초기화`) 조작 지원.

---

## 📂 프로젝트 주요 산출물 구조
```
buy-the-dip/
├── CLAUDE.md                   # 하네스 포인터 파일
├── memory.md                   # 세션 기록 메모리 파일 (본 파일)
├── server.py                   # 실제 주가 수집 API 및 정적 파일 웹 서버 (Python 8080)
├── index.html                  # 시뮬레이터 대시보드 HTML
├── style.css                   # Glassmorphism CSS 디자인 시스템
├── app.js                      # 시뮬레이션 백테스트 엔진 & Chart.js 줌/패닝 제어
├── .claude/
│   ├── agents/                 # 전담 에이전트 정의 4종
│   │   ├── strategy-architect.md
│   │   ├── quant-backtester.md
│   │   ├── risk-manager.md
│   │   └── execution-developer.md
│   └── skills/                 # 오케스트레이터 및 분석 스킬
│       ├── dip-strategy-orchestrator/SKILL.md
│       └── dip-backtest-analyzer/SKILL.md
```
